<div class="custom-topbar">
    <div class="custom-topbar__title">
        <h2>Dashboard</h2>
    </div>
    <div class="custom-topbar__actions">
        <button class="custom-topbar__profile-btn">👤 Profile</button>
        <form asp-controller="Dashboard" asp-action="Settings" method="get">
            <button type="submit" class="custom-topbar__settings-btn">⚙️ Settings</button>
        </form>
        <form asp-controller="Login" asp-action="Login" method="get">
            <button type="submit" class="custom-topbar__logout-btn">🚪 Logout</button>
        </form>
    </div>
</div>

<style>
    .custom-topbar {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 8px;
        background-color: #29699A;
        color: white;
        font-family: Arial, sans-serif;
        z-index: 1000;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
    }

    .custom-topbar__title h2 {
        margin: 0;
        font-size: 20px;
        font-weight: bold;
    }

    .custom-topbar__actions {
        padding-right: 30px;
        display: flex;
        gap: 20px;
    }

        .custom-topbar__actions button {
            background: white;
            color: #29699A;
            border: none;
            padding: 4px 10px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            font-size: 0.75rem;
            transition: background 0.3s ease, transform 0.2s ease;
        }

            .custom-topbar__actions button:hover {
                background: #f0f0f0;
                transform: scale(1.05);
            }

            .custom-topbar__actions button:active {
                transform: scale(0.95);
            }

</style>
